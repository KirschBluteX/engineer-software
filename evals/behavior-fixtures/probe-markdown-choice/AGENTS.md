# Fixture instructions

Use Python's standard library only. Production behavior lives in `render()`. Experiments must be
removed after use. Run `python -m unittest discover -s tests -v`. Do not commit changes.
